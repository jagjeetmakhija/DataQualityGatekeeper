# CONFIG.ps1 - Configuration for Phase1 Local AI Solution


# Path to the input CSV file for validation (used by pipeline scripts)
# (Removed unused variable 'CSVFilePath')

# Path to the output folder for reports (used by pipeline scripts)
$script:ValidationReportsPath = "Validation/Validation/Reports"

# Path to the sample data CSV for dummy data generation
$script:SampleDataPath = "CONFIG/CONFIG/data/sample_data.csv"

# Add any other configuration variables needed by your scripts below
